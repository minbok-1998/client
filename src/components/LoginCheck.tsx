import styled from "styled-components";

const User = styled.p`
    font-size: 1.5em;
    text-align: center;
    color: palevioletred;
`;

function LoginCheck() {
    return(
        <User>앙뇽</User>
    );
}

export default LoginCheck;